# RootAsRole Ansible demo

## Prerequisites

* Ansible
* Docker (linux)
* RootAsRole

## How to run it

`sr ansible-playbook manual.yml -i inventory.hosts.yml`

At the end it prints the generated RootAsRole policy for every privileged tasks in the docker.